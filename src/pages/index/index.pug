extends layout/page

block append head
    link(href=require('./styles.scss'), rel='stylesheet')

block append main
    section.section
        button.button.button__brown(data-action='popup', data-popup='order') Попап оформления заказа

block append scripts
    script(src=require('./main.ts'))
